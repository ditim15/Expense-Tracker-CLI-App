[build-system]
requires= ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name="expense-tracker"
version = "1.0.0"
requires-python = ">=3.10"

[project.scripts]
tasks = "expense_tracker.main:main"